export enum SubscribersPropDesc {
  Email = 'Unique user email',
  FirstName = 'User firstname',
  LastName = 'User lastname',
}

export enum SubscribersApiDesc {
  GetAll = 'Return list of subscribers',
  GetOne = 'Return subscriber by id',
  Create = 'Create subscribers',
  Remove = 'Delete subscribers by id',
  RemoveAll = 'Delete all subscribers',
}
